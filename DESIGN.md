# PC DESIGN.md

> Version: 1.0
> Status: Internal
> Scope: PC 后台管理 / 门户首页 / 驾驶舱模板 / 数据看板 / 大屏
> Base: Element UI（二次封装体系）
> Source: 基于现有产品截图沉淀（CRUD 列表、编辑弹窗、门户首页、模板管理、亮色看板、深色大屏）

---

## 0. 本文档的定位

本设计规范不是一个通用审美说明，而是面向当前产品体系的 **“AI 可执行设计约束”**。
目标不是生成“好看但不像我司产品”的页面，而是让设计、前端、AI、低代码平台、代码生成器都能稳定产出 **像同一个系统、同一家公司、同一组件体系** 的 PC 页面。

本文档适用于：

- 常规后台 CRUD 页面
- 表单、详情、编辑弹窗
- 门户首页 / 工作台
- 驾驶舱模板管理页
- 亮色数据看板
- 深色大屏（参考性约束，不做过度限制）

本文档不适用于：

- 营销官网
- 活动页
- 强视觉品牌宣传页
- 移动端页面（另立规范）

---

## 1. 产品视觉基因（基于现有页面提炼）

### 1.1 设计基调

本产品不是互联网消费型 UI，而是典型的 **企业级业务系统 UI**，设计重点是：

1. **稳定优先**
   - 页面结构稳定，用户不需要重新学习
   - 导航位置、表格结构、弹窗逻辑保持一致

2. **业务优先**
   - 信息表达清楚，比“视觉炫技”更重要
   - 页面需要支撑高频操作、批量处理、筛选、查看、配置

3. **密度适中偏高**
   - 相比营销站或轻量工具，信息密度更高
   - 但不是极端密集，仍保留适度留白和卡片边界

4. **Element UI 血缘明显，但不是原生样式**
   - 组件尺寸、交互范式、表单结构、表格逻辑与 Element UI 一致
   - 但颜色、圆角、边距、卡片风格、导航层次、看板表达均已做二次封装

5. **风格分层明确**
   - 后台管理页：浅色、克制、规整
   - 门户首页：轻内容化、带一点品牌感
   - 驾驶舱模板：卡片化资源管理
   - 亮色数据看板：浅底图表与经营数据
   - 深色大屏：独立视觉语言，科技感更强

---

## 2. 设计原则（必须遵守）

### 2.1 一致性优先于局部创新

- 同类页面结构必须一致
- 同类组件视觉必须一致
- 同类操作入口必须一致
- 不允许为了单页“看起来高级”而破坏系统一致性

### 2.2 先解决业务可用，再讨论装饰

页面优先级如下：

1. 用户能快速找到功能
2. 用户能读懂数据和状态
3. 用户能安全完成操作
4. 页面看起来整洁专业
5. 最后才是装饰与风格增强

### 2.3 默认使用成熟模式，不自创交互

本体系建立在企业软件常见公约之上，默认采用成熟模式：

- 左侧导航 + 顶部全局区
- 页签 / 面包屑 / 列表 / 搜索区 / 分页
- 表单分组
- 弹窗编辑
- 卡片式看板
- 图表 + 指标卡
- 大屏模块化布局

不做“为创新而创新”的跳脱布局。

### 2.4 操作分级明确

页面动作必须分级：

- **主操作**：页面唯一主按钮（蓝色）
- **次操作**：普通按钮 / 描边按钮 / 默认按钮
- **辅助操作**：文本链接、更多、图标按钮
- **危险操作**：删除、清空、停用、覆盖等，必须有确认

### 2.5 信息层级清楚

一屏内必须让用户一眼区分：

- 全局导航
- 页面上下文
- 检索/筛选
- 主数据区
- 配置区
- 反馈状态
- 操作入口

不得将所有信息做成同一视觉权重。

---

## 3. 视觉体系总览

### 3.1 风格分层

本产品分为 5 类视觉场景：

#### A. 后台管理页（图1、图2）
关键词：

- 标准化
- 规整
- 高可用
- 浅色
- 中高信息密度
- 操作导向

#### B. 门户首页 / 工作台（图3）
关键词：

- 内容聚合
- 白卡片
- 品牌头图
- 信息入口
- 轻运营感

#### C. 模板管理 / 资源管理（图4）
关键词：

- 卡片陈列
- 缩略图优先
- 批量操作
- 筛选 + 资源浏览

#### D. 亮色经营看板（图5）
关键词：

- 数据监控
- 指标卡
- 图表
- 表格联动
- 浅色经营风格

#### E. 深色大屏（图6）
关键词：

- 科技感
- 深色背景
- 发光边框
- 视觉冲击
- 模块仪表化

---

## 4. 设计 Token（主规范）

> 说明：以下是推荐的设计 token 命名方式，可直接映射到主题变量、CSS Variables 或组件封装参数。

---

## 5. 色彩系统

### 5.1 品牌主色（浅色后台主体系）

后台主体系的视觉核心仍然是 **蓝色主导**，与 Element UI 生态兼容，但比原生更偏“稳定企业感”。

#### Primary Blue

- `--color-primary-1: #1E80FF` 主按钮、主选中、关键链接
- `--color-primary-2: #409EFF` 兼容态/强调态
- `--color-primary-3: #D9ECFF` 浅底强调
- `--color-primary-4: #ECF5FF` 浅浅色背景

> 使用建议：
> - 主按钮：`#1E80FF`
> - Tabs 激活、页签高亮、页内主链接：`#1E80FF`
> - 选中背景、模块轻强调：`#ECF5FF`

### 5.2 中性色（后台基础）

#### Background

- `--bg-page: #F5F7FA` 页面底色
- `--bg-section: #F7F8FA` 次级区域底色
- `--bg-card: #FFFFFF` 卡片、弹窗、面板
- `--bg-hover: #F5F7FA` hover 轻底色
- `--bg-disabled: #F5F7FA` 禁用背景

#### Border

- `--border-color-base: #EBEEF5`
- `--border-color-light: #F0F2F5`
- `--border-color-dark: #DCDFE6`

#### Text

- `--text-primary: #303133`
- `--text-regular: #606266`
- `--text-secondary: #909399`
- `--text-placeholder: #C0C4CC`
- `--text-disabled: #C8CDD4`

### 5.3 功能色

- `--color-success: #67C23A`
- `--color-warning: #E6A23C`
- `--color-danger: #F56C6C`
- `--color-info: #909399`

> 规则：
> - 成功：启用、通过、完成、在线
> - 警告：待处理、风险、提醒、阈值预警
> - 危险：删除、停用、失败、异常
> - 信息：中性提示、说明

### 5.4 门户与看板扩展色

亮色看板中，可引入业务图表色：

- `--chart-blue: #5B8FF9`
- `--chart-cyan: #5AD8A6`
- `--chart-green: #61DDAA`
- `--chart-orange: #F6BD16`
- `--chart-red: #E8684A`
- `--chart-purple: #9270CA`

### 5.5 深色大屏色板（独立视觉域）

深色大屏不要直接复用后台浅色 token，应独立管理。

#### Background

- `--screen-bg: #061326`
- `--screen-panel: rgba(10, 27, 58, 0.75)`
- `--screen-panel-deep: rgba(7, 18, 40, 0.92)`

#### Accent

- `--screen-blue: #2F7BFF`
- `--screen-cyan: #4FE3FF`
- `--screen-teal: #42D7C5`
- `--screen-gold: #F5B84B`
- `--screen-green: #4DE58E`

#### Text

- `--screen-text-primary: #EAF4FF`
- `--screen-text-secondary: rgba(234, 244, 255, 0.78)`
- `--screen-text-weak: rgba(234, 244, 255, 0.55)`

> 注意：大屏仅作为参考视觉，不要求 CRUD 体系与大屏统一。

---

## 6. 字体与排版

### 6.1 字体族

中文优先使用系统无衬线字体：

```text
PingFang SC, Microsoft YaHei, Helvetica Neue, Arial, sans-serif
```
